{{#> layout-public }}
  {{#* inline "head" }}
    <title>Successful - BPM</title>
  {{/ inline }}

  {{#* inline "content" }}
    <div class="position-middle">
      <div>
        <fieldset>
          <h1>Success</h1>
          <small>{{ this }}</small>
        </fieldset>
      </div>
    </div>
  {{/ inline }}
{{/ layout-public }}